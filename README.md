This is an application home assessment for and interview the following will be steps on how to run/install and use it(altought will be hosted as well).

## Getting Started
First install the required packages(node_modules): 
```
```
```bash 

npm install
```

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.


This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font), a new font family for Vercel.

## Deploy on Vercel

The project will be deployed on Vercel. But later in the development.
